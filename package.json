{
  "name": "codex-remote-relay",
  "description": "Authenticated local relay and remote web client for Codex app-server.",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/omrijsharon/vscode-copilot-bridge.git"
  },
  "main": "./out/relay/server.js",
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "lint": "tsc -p . --noEmit",
    "start": "node out/relay/server.js",
    "start:relay": "node out/relay/server.js",
    "test": "npm.cmd run build && node --test .\\tests\\*.test.js"
  },
  "dependencies": {
    "@types/qrcode": "^1.5.6",
    "qrcode": "^1.5.4",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "@types/ws": "^8.18.0",
    "typescript": "^5.8.2"
  }
}
